# CallService Validation
callSession.channelType.required=Channel type (DIRECT, GROUP) is required
callSession.mediaType.required=Media type (AUDIO, VIDEO) is required
callSession.hostUserId.required=Host user ID is required
callSession.status.required=Call status is required
callParticipant.callSessionId.required=Call session ID is required
callParticipant.userId.required=User ID is required
callParticipant.role.required=Participant role is required
callParticipant.status.required=Participant status is required
